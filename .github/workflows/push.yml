name: push

on:
  push:

jobs:
  push:
    runs-on: ubuntu-latest
    env:
      FOSSA_API_KEY: ${{ secrets.FOSSA_API_KEY }}

    steps:
      - uses: actions/checkout@v2
        
      - name: Check whether setting up repo
        run: |
          if [ -f .github/workflows/setup.yml ]; then
            echo "::set-env name=setup::false"
          else
            echo "::set-env name=setup::true"
          fi

      - name: Run Fossa and upload data
        if: env.FOSSA_API_KEY != '' && env.setup
        uses: fossa-contrib/fossa-action@v1
        with:
          fossa-api-key: ${{ env.FOSSA_API_KEY }}
    
      - name: Install dependencies
        if: env.setup
        run: |
          pip install pipreqs
          if [ -f requirements.txt ]; then
            pip install -r requirements.txt
          fi

      - name: Run pipreqs
        if: env.setup
        run: pipreqs --force

      - name: Commit changes if needed
        if: env.setup
        run: |
          if [ $(git diff --quiet HEAD || echo $?) -ne 0 ]; then
            git add .
            git config user.name "${{github.event.pusher.name}}"
            git config user.email "${{github.event.pusher.email}}"
            git commit -m "[chore] Update requirements"
            git push origin master
          fi
